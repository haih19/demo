export enum Units {
  standard = "standard",
  metric = "metric",
  imperial = "imperial",
}

export enum HeaderItem {
  unitPreference = "unit-preference",
}

export enum KeyboardCode {
  enter = "Enter",
}
